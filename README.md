# Project Setup
```bash
brew install pkg-config
brew install gtk+3
brew uninstall --ignore-dependencies glib
brew install https://raw.githubusercontent.com/tschoonj/homebrew-core/glib-2.58.3/Formula/glib.rb
```
